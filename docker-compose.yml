version: '3'
services:
  admin_app:
    build: ./admin-app
  
  api:
    build: ./api
    environment:
      - DB_HOST=mysql
      - DB_PORT=3306
    depends_on:
      - mysql
  
  game:
    build: ./game
    
  php_executor:
    build: ./php-server
  
  nginx:
    build: ./nginx-proxy
    ports:
      - "80:80"
    depends_on:
      - admin_app
      - api
      - game
      - php_executor

  mysql:
    image: mysql:8.0
    volumes:
      - mysql_data:/var/lib/mysql
      - ./lost_in_code.sql:/docker-entrypoint-initdb.d/schema.sql
    env_file:
      - ./.env.db
  
volumes:
  mysql_data:
